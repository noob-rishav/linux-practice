# basic commands related to the permissions in the linux
1. chmod (used to add or remove permissions)
2. chown (changes the owner , for that we will have to be the root user)
4. sudo (used to make us the root user)
5. ls -l (list all the permissions related to the different files inside of the directory)